public enum MatchType
{
    Local,   // Two players on the same machine, same instance (hotseat)
    LAN,     // Two players connected over local network
    Online,  // Two players connected over external network
    AI       // One local player, one AI
}
